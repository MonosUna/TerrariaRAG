# TerrariaRAG — RAG-система по Terraria

TerrariaRAG — это многоагентная Retrieval-Augmented-Generation система, построенная на данных из **[Terraria Wiki](https://terraria.wiki.gg/ru)** и предназначенная для точных ответов на вопросы игроков:

* крафт предметов
* механики
* враги
* биомы
* аксессуары
* материалы
* НИПы
* боссы
* и т. д.

Система полностью автономна: от подготовки данных до генерации ответа.

Статус: MVP+

# Архитектура TerrariaRAG

TerrariaRAG использует многоагентную архитектуру:

---

## 1. Redirect-модель (LLM)

Определяет, какие агенты должны решать задачу.
Например:

> Как скрафтить Ночное Лезвие и где его применяют?

Разбивается на:

* CraftAgent → крафт
* GeneralAgent → применение

---

## 2. Агенты

### CraftAgent

Агент, специализирующийся на крафте:

* ищет информацию в адаптированной ChromaDB
* дополняет контекстом из `recipes.json`
* приводит рецепты к нормальной структуре
* вызывает LLM для генерации финального ответа

### GeneralAgent

Общий агент:

* механики игры
* правила
* монстры
* биомы
* предметы
* блоки и материалы
* NPC, боссы и т.п.

Использует свою ChromaDB.

### MistralLLM

Универсальная обёртка вокруг клиента Mistral API:

* передаёт системные/пользовательские сообщения
* контролирует температуру
* логирует запросы и ответы
* переиспользуется во всех агентах

---

## 3. Merge-модель

После того как агенты ответили — отдельная модель объединяет ответы в один гладкий, логичный ответ.

---

# Установка

## 1. Установить зависимости

```
pip install -r requirements.txt
```

## 2. Создать `.env`

```
API_KEY=ВАШ_API_КЛЮЧ_MISTRAL
```

## 3. Создать векторные базы

### Общая база:

```
python manage_db.py create \
  --json_path data/general.json \
  --persist_directory terraria_db/general \
  --use_cuda
```

### База рецептов:

```
python manage_db.py create \
  --json_path data/recipes.json \
  --persist_directory terraria_db/recipes \
  --use_cuda
```

Важно: JSON должен содержать поле `"content"`.

---

# Запуск

```
python main.py
```

CLI попросит ввести вопрос.
Можно сразу ввести что-то вроде:

```
Как получить Облачные ботинки и какие есть улучшения?
```

---

# 💡 Примеры запросов

```
Как скрафтить Мастерское щитоносное кольцо?
```

```
Какие биомы появляются после убийства Стены плоти?
```

```
Сколько хлорофита нужно на полный комплект брони?
```

```
Как работает миниакула?
```

---

# Работа с ChromaDB

`manage_db.py` можно использовать и как библиотеку, и как CLI. Однако, CLI не поддерживает update_db в силу большого количества параметров

### Создать базу:

```
python manage_db.py create --json_path ... --persist_directory ...
```

### Удалить:

```
python manage_db.py delete --persist_directory ...
```

> Важно: можно свободно импортировать `create_db` или `delete_db` в другие файлы — функции изолированы и не зависят от CLI.

---

Больше информации можно получить если запустить с флагом `-h` или `--help`:

```
python manage_db.py --help
```

# Подготовка данных

Wiki-страницы обрабатываются:

* парсером таблиц
* парсером шаблонов
* удалением HTML
* нормализацией текста

После обработки результат сохраняется в `wiki_dump_cleaned.json`.

---

# 🤝 Контрибьютинг

PR-ы приветствуются, особенно:

* улучшение парсинга
* НОВЫЕ ДАТАСЕТЫ РЕЦЕПТОВ

---

# Создатели

* [nvclon](https://github.com/nvclon)
* [georgens2004](https://github.com/georgens2004)
* [MonosUna](https://github.com/MonosUna)
* [добавить]
* [добавить]

# 📄 Лицензия

MIT License.

